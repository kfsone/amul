#pragma once
// Type aliases

#include <cstdint>

// "condition" instruction identifier
using condid_t = int;

// "action" instruction identifier
using actionid_t = int;

// identifies a user-defined verb
using verbid_t = int;

// offset of a message in the messages file
using umsgoff_t = size_t;