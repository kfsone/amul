#pragma once

#include <cstdint>

int  ttproc();
void act(int32_t ac, int32_t *pt);
int  cond(int32_t n, int l);
int  type(char **s);
int  actual(uint32_t n);
int  actptr(uint32_t n);
void deduct(int plyr, int points);