#ifndef SMUGL_SMUGL_LANGTABLE_HPP
#define SMUGL_SMUGL_LANGTABLE_HPP

#include "parser.hpp"

bool do_condition(struct VBTAB *vt, bool lastCond);
slotResult do_action(struct VBTAB *vt, bool lastCond);

#endif  // SMUGL_SMUGL_LANGTABLE_HPP
